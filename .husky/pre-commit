#!/usr/bin/env sh
# pre-commit hook: 커밋 전 코드 품질 검사

# 스테이지된 파일에 대해 lint-staged 실행 (ESLint + Prettier)
npx lint-staged

# TypeScript 전체 타입 체크 수행
# (lint-staged에서 tsc를 개별 파일로 실행하면 tsconfig.json이 무시됨)
npm run type-check
